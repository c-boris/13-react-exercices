import React from 'react';

const BigNumber = ({ count }) => {
  return (
    <div className="BigNumber">
      <h2>Big Number</h2>
      <p>Current Count: {count}</p>
    </div>
  );
};

export default BigNumber;